import React from "react";
import style from "./ButtonComps.module.css";

export default function StreamingServiceButton() {
  return (
    <div className={`${style.btn_streamingservice} ${style.on}`}>
    </div>
  );
}