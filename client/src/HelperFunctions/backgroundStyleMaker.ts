const backgroundStyle = (url: string) => {
  return {
    background: `linear-gradient(0deg, rgba(255, 255, 255, 0.6), rgba(255, 255, 255, 0.6)), url(${url})`,
  };
};

export default backgroundStyle;
